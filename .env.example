# Example environment variables for Yahoo Finance Latest → DynamoDB uploader
# Copy this file to `.env` (or export variables in your shell) and adjust as needed.

# DynamoDB table name (default used by the script if unset: news_items)
NEWS_TABLE=news_items

# AWS region for DynamoDB (e.g., us-east-1)
AWS_REGION=us-east-1

# Optional: If you prefer using AWS_DEFAULT_REGION instead of AWS_REGION
# AWS_DEFAULT_REGION=us-east-1

# Optional: Use a specific AWS CLI profile (credentials are resolved by AWS SDK default chain)
# AWS_PROFILE=default

# Optional: Direct credentials (not recommended for committed files; use profiles/role instead)
# AWS_ACCESS_KEY_ID=YOUR_KEY_ID
# AWS_SECRET_ACCESS_KEY=YOUR_SECRET
# AWS_SESSION_TOKEN=YOUR_SESSION_TOKEN

